name: Deploy to Firebase Hosting + Cloud Run on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: npm ci

      - name: Install Functions dependencies
        run: npm ci --prefix functions

      - name: Build
        run: npm run build

      - name: Build Functions
        run: npx tsc -p functions/tsconfig.json

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_JOBFILTER_UK }}'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy API to Cloud Run
        run: |
          gcloud run deploy jobfilter \
            --source . \
            --region europe-west2 \
            --platform managed \
            --allow-unauthenticated \
            --min-instances 0 \
            --max-instances 10 \
            --memory 512Mi \
            --cpu 1 \
            --timeout 60 \
            --set-env-vars NODE_ENV=production \
            --update-secrets STRIPE_SECRET_KEY=STRIPE_SECRET_KEY:latest \
            --project jobfilter-uk

      - name: Deploy Functions and Hosting
        run: npx firebase-tools deploy --only functions,hosting --project jobfilter-uk
